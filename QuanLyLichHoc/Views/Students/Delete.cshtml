@model QuanLyLichHoc.Models.Student
@{
    ViewData["Title"] = "Xóa Học sinh";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-danger shadow border-0 rounded-4">
                <div class="card-header bg-danger text-white text-center py-3 rounded-top-4">
                    <h5 class="mb-0 fw-bold"><i class="bi bi-exclamation-triangle-fill me-2"></i>XÁC NHẬN XÓA HỒ SƠ</h5>
                </div>

                <div class="card-body p-4 text-center">
                    <h5 class="card-title text-danger mb-3">Bạn có chắc chắn muốn xóa học sinh này?</h5>

                    <div class="alert alert-warning text-start small border-0 bg-warning bg-opacity-10 text-dark">
                        <i class="bi bi-info-circle-fill me-1"></i>
                        <strong>Cảnh báo:</strong> Hành động này không thể hoàn tác.
                        Tài khoản đăng nhập, điểm danh và các kết quả học tập liên quan của học sinh này cũng sẽ bị xóa vĩnh viễn khỏi hệ thống.
                    </div>

                    <div class="list-group list-group-flush text-start mb-4 shadow-sm rounded-3">
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold text-secondary">Mã Sinh Viên:</span>
                            <span class="fw-bold text-dark">@Model.StudentCode</span>
                        </div>
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold text-secondary">Họ và Tên:</span>
                            <span class="fw-bold text-primary">@Model.FullName</span>
                        </div>
                        <div class="list-group-item d-flex justify-content-between align-items-center">
                            <span class="fw-bold text-secondary">Lớp:</span>
                            <span>
                                @if (Model.Class != null)
                                {
                                    <span class="badge bg-light text-dark border">@Model.Class.ClassName</span>
                                }
                                else
                                {
                                    <span class="text-muted">Chưa có lớp</span>
                                }
                            </span>
                        </div>
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-flex justify-content-center gap-3">
                            <a asp-action="Index" class="btn btn-secondary rounded-pill px-4">
                                <i class="bi bi-x-lg me-1"></i> Hủy bỏ
                            </a>
                            <button type="submit" class="btn btn-danger rounded-pill px-4 shadow-sm fw-bold">
                                <i class="bi bi-trash-fill me-1"></i> Xóa vĩnh viễn
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>